# Native

!!! info "Prerequisites"

    Ensure you're using Python 3.12.

Install Vero's dependencies using the package manager of your choice:

```bash
pip install -r requirements.txt
```

or

```bash
uv sync
```

You should now be able to run Vero:

```bash
python src/main.py <arguments>
```
